interface:
  display_name: "Openmind"
  short_description: "Repo-specific patterns and workflows for openmind"
  default_prompt: "Use the openmind repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true